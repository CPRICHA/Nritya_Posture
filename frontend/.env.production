# Vercel: use same-origin proxy (vercel.json rewrites /api → Render)
VITE_API_URL=/api
VITE_USE_API_PROXY=true

# Direct URL (only if NOT using Vercel proxy):
# VITE_API_URL=https://nritya-backend-vcgv.onrender.com
