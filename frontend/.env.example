# Copy to .env for local overrides (optional)
# Production builds use .env.production automatically.

# Deployed backend (Render)
VITE_API_URL=https://nritya-backend-vcgv.onrender.com

# Local backend (use with npm run dev)
# VITE_API_URL=http://127.0.0.1:8000
